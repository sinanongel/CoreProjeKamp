<head>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js" type="text/javascript"></script>
</head>
<div class="tech-btm">
    <form method="post">
        <input type="email" id="txtmail" placeholder="Email" name="Mail" class="tech-btm">
        <input type="submit" id="btn" value="Abone Ol">
        <br />
        <div id="sonuc"></div>
        <br />
    </form>
</div>

<script type="text/javascript">
    $("#btn").click(function (e) {
        let values = {
            Mail: $("#txtmail").val()
        };
        alert(values);
        var bolum = document.createElement("div");
        bolum.className = "alert alert-success";
        bolum.innerHTML = "Kayıt Başarılı";
        document.body.appendChild(bolum);
        e.preventDefault();
        var veri = $("#subForm").serialize();
        $.ajax({
            type: "post",
            url: "/NewsLetter/SubscribeMail/",
            data: values,
            success: function (func) {
                $("#sonuc").html(bolum);
                let result = jQuery.parseJSON(func);
            }
        });
    });
</script>


@*<div class="tech-btm">
        <p>Teknolojii yazılım ve diğer bloglar...</p>
        <form id="subForm" method="post">
            <input type="email" id="Email" placeholder="Email" required="" name="Mail">
            <input type="submit" id="btn" value="Abone Ol">
            <br />
            <div id="sonuc"></div>
            <br />
        </form>
    </div>

    <script type="text/javascript">
        $(function () {
            $("#btn").click(function (e) {
                var bolum = document.createElement("div");
                bolum.className = "alert alert-success";
                bolum.innerHTML = "Kayıt Başarılı";
                document.body.appendChild(bolum);
                e.preventDefault();
                var veri = $("#subForm").serialize();
                $.ajax({
                    type: "post",
                    url: "/NewsLetter/SubscribeMail/",
                    data: veri,
                    success: function (f) {
                        $("#sonuc").html(bolum);
                        setTimeout(() => { window.location.reload() }, 7500);
                    }
                });
            });
        });
    </script>*@